{
  "name": "@platform/design",
  "version": "0.0.0",
  "private": true,
  "description": "Design tokens — encodes the FROZEN UI design system (docs/ui/DESIGN_SYSTEM.md). Do not redesign.",
  "type": "module",
  "sideEffects": ["**/*.css"],
  "exports": {
    ".": "./src/index.ts",
    "./styles.css": "./src/styles.css"
  },
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "echo \"no tests yet\""
  },
  "devDependencies": {
    "@platform/eslint-config": "workspace:*",
    "@platform/tsconfig": "workspace:*",
    "eslint": "^9.13.0",
    "typescript": "^5.6.3"
  }
}
